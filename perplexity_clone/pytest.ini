[pytest]
# `pythonpath = .` permet aux tests d'importer les modules du projet
# (api, pipeline, cache…) sans bidouiller sys.path.
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
